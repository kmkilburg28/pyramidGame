using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutorialBones : MonoBehaviour {
   

	// Use this for initialization
	void Start () {
        PlayerPrefs.SetInt("Player Score", 23 );
    }

    // Update is called once per frame
    void Update () {
		
	}
}
